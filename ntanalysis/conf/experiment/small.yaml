# @package _global_

artifacts:
    enable_logger: false
    experiment_name: predict_nt_small

data:
    max_dataset_length: 0.01

train:
    num_warmup_steps: 120
    num_training_steps: 1800
    log_every_n_steps: 64
