# @package _global_

artifacts:
    enable_logger: true
    experiment_name: predict_nt_close

data:
    batch_size: 512
    max_dataset_length: 0.75
    input_gap: 200

train:
    num_warmup_steps: 4800
    num_training_steps: 65536
    log_every_n_steps: 512
