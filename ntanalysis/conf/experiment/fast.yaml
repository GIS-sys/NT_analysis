# @package _global_

artifacts:
    enable_logger: false
    experiment_name: predict_nt_fast

data:
    batch_size: 64
    max_dataset_length: 0.05

train:
    num_warmup_steps: 500
    num_training_steps: 1000
