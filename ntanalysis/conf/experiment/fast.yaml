# @package _global_

artifacts:
    enable_logger: false
    experiment_name: predict_lpc_fast

data:
    batch_size: 1
    max_dataset_length: 0.05

train:
    num_warmup_steps: 500
    num_training_steps: 1000
